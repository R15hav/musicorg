"""musicorg — terminal music organizer + canonical metadata + lossless upgrade.

Linux-targeted. The package is the core library; the CLI is at
``musicorg.cli`` and the Textual TUI is at ``musicorg.cli.tui``.
"""

__version__ = "0.1.0"
